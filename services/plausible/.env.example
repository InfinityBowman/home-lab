# Must match the BASE_DOMAIN in infrastructure/.env
BASE_DOMAIN=jacobmaynard.dev

# CRITICAL: Used for session signing and TOTP vault. If lost, users must re-authenticate.
# Generate with: openssl rand -base64 48
PLAUSIBLE_SECRET_KEY_BASE=

# PostgreSQL password
PLAUSIBLE_POSTGRES_PASSWORD=
